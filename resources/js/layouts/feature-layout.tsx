import FeatureLayoutTemplate from '@/layouts/feature/feature-default-layout';
import type { FeatureLayoutProps } from '@/types';

export default (props: FeatureLayoutProps) => (
   <FeatureLayoutTemplate {...props} />
);
